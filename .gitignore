# Rust
/target
**/*.rs.bk
# Cargo.lock is committed: vitals is a binary crate, so pinning the full
# transitive dependency set is what makes release builds reproducible.

# Local runtime state / secrets
/config.toml
*.env
/data
*.db
*.db-wal
*.db-shm

# OS / editor
.DS_Store
